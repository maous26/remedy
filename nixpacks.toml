# Nixpacks configuration for Railway

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]
dependsOn = ["install"]

[start]
cmd = "NODE_ENV=production node_modules/.bin/next start -H 0.0.0.0 -p ${PORT:-3000}"
