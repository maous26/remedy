# Nixpacks configuration for Railway

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.build]
# Clear server/static chunks before build to avoid stale cache issues
cmds = ["find .next -name '*.js' -path '*/server/*' -delete 2>/dev/null || true", "npm run build"]

[start]
cmd = "npm run start"
